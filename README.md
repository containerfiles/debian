# baseline-debian

Minimal Debian stable base container.

## Usage

```bash
baseline-debian              # Interactive shell
baseline-debian <command>    # Run specific command
```

## What's Inside

- Debian stable-slim
- bash shell
- Standard GNU utilities

## Notes

Good base for containers needing apt packages or glibc compatibility.
